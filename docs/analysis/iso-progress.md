# Refactor isométrico — página de progreso en vivo

**Rama:** `feature/isometric` · **Iniciado:** 2026-08-25 · Actualizada por los agentes tras cada ronda.
Vista en vivo de agentes: `/workflows` en Claude Code.

## Método

Cada pieza la implementa un **builder** y la juzga un **crítico ciego con contexto fresco** que
ejecuta el juego real (`tools/screenshot_runner.gd` → PNGs) y compara dos versiones etiquetadas
A/B (sin saber cuál es cuál) contra el lenguaje visual de los RTS isométricos clásicos.
Si la nueva pierde, el crítico nombra **el mayor hueco** y el builder vuelve a entrar.
Sin número fijo de rondas (tope de seguridad: 4/pieza, se anota si se alcanza).
Entre olas, un agente fresco juega la partida completa y alisa incoherencias.

## Piezas

| # | Pieza | Estado | Rondas | Último veredicto |
|---|---|---|---|---|
| P0 | Proyección isométrica de cámara + picking + límites de cámara | ✅ aprobada por crítico | 3 | la nueva gana — gap: "Buildings are still flat, front-facing billboards: the Town Center is a square facade sitting on a horizontal rectangular base that ignores the projected ground. It needs an isometric massing — a diamond footprint with two visible side faces (roof plane + shaded walls) so it occupies the same 2:1 diamond space as the terrain. This is the single element that most breaks the isometric illusion, since the building is the focal point of every shot." |
| P1 | Unidades verticales (billboard): sprites, sombras, barras HP, selección | ✅ aprobada por crítico | 1 | la nueva gana — gap: "Set A still draws a fully opaque, saturated team-colour ellipse under every unit at all times, so sheep look like they stand in ponds and selected vs unselected units are indistinguishable; it should be a thin outlined ellipse shown only when selected, with a subtle soft ground shadow otherwise." |
| P2 | Edificios verticales: billboard, orden Y (y-sort), preview de colocación | ⏳ en cola | 0 | — |
| P3 | Legibilidad del terreno bajo proyección: costas, agua, scatter, viñeta | ✅ aprobada por crítico | 1 | la nueva gana — gap: "The base grass still reads as one flat vector-green sheet: terrain zones are soft-alpha olive blotches that look like stains rather than terrain. It needs tile-scale texture variation (mottled grass tones, dirt speckle) and harder, dithered zone borders so mid-zoom ground reads like an AoE2-era tileset instead of blank green. (Water/coast could not be judged: seed 42 produces a landlocked map in all four shots.)" |
| P4 | Integración HUD/mapa: rect de cámara del minimapa, marcadores, dpad | ✅ aprobada por crítico | 2 | la nueva gana — gap: "The minimap world render is not fitted to its frame: in normal-zoom shots the map diamond fills only ~25% of the panel, offset to the bottom-right corner, leaving most of the framed area as empty black grid — scale and center the minimap content to fill the panel like classic isometric RTS minimaps." |
| S1 | Alisado inter-olas (partida completa) | ✅ pasada | 1 | presentación coherente en 3 mapas sin SCRIPT ERROR; corregidos: sombras desalineadas, oveja convertida repintada entera, rect de cámara del minimapa sin proyectar, banderín de rally tumbado y "0%" huérfano en el HUD |
| S2 | Alisado final + crítico de experiencia completa | ⏳ en cola | 0 | — |

## Registro de rondas

- **P3 ronda 1** (nueva GANA, run_ok=true): "Run exited 0 with no SCRIPT ERROR lines; all 4 PNGs saved. Set A (/tmp/calima-iso/p3-r1) is the stronger build in the piece's area. Concrete wins seen in magnified crops: (1) mountains in A render as legible snow-capped grey cones with faceted rock skirts, while in B the same map features are flat grey smears with a white speck that are unreadable at overview zoom; (2) rock/stone scatter in A has faceted light/shadow shading and clusters that read as boulder piles at close and medium zoom, plus visible gold-nugget piles, versus flatter, smaller grey specks in B; (3) forest-floor dark patches hug tree clusters more tightly in A. No regressions detected in A: HUD resource bar, population, age label, speed buttons, minimap, unit sprites, TC, health/selection markers all intact; no black frames or missing entities. Minor baseline-only artifact: B's 01_tc_close has a clipped white shape stuck at the top HUD edge. Remaining weaknesses common to both: jagged stair-step map edges against void in medium shots, and no water/coast present on this seed so those claimed improvements are unverified."
- **P4 ronda 2** (nueva GANA, run_ok=true): "Run exited 0 with no SCRIPT ERROR lines (only benign engine warnings). Set A adds a bordered, opaque minimap panel with grid background and a correct white diamond viewport indicator in all four shots; the bottom HUD band is fully opaque. Set B's minimap is frameless and its bottom band is semi-transparent, letting dimmed world geometry bleed through (01/02/04) and smearing the overview minimap into an unbordered green-speckled region with a stray black blob — reads as broken compositing. World rendering, units, buildings, and top resource bar are identical between sets, so A introduces no regressions elsewhere. No visible order/move markers in either set, so panning/marker polish could not be differentiated beyond the viewport rectangle, which works in both."
- **P1 ronda 1** (nueva GANA, run_ok=true): "Run exited 0 with no SCRIPT ERROR lines. A vs B differs only in the under-unit marker: B uses flat axis-aligned blue rectangles that read as floating platforms/stray HP bars, while A uses perspective-correct ground ellipses under villagers, mounted scout, and sheep — the proper isometric-RTS ground-contact idiom, so A wins the piece area. Orientation is equivalent in both sets (units/animals face plausible travel directions); HP bars are not observable (no damaged units, and correctly no bars at full HP). No regressions found in A: HUD, minimap, fog, terrain, and overview frames match B with no black frames, missing entities, or new artifacts."
- **P4 ronda 1** (nueva PIERDE, run_ok=true): "Run exited 0 with no SCRIPT ERROR lines. Blind A/B: all four pairs are near-identical; pixel diffs (numpy on ImageChops) localize exclusively to wandering sheep/animals, villager animation phase, and matching minimap dot shifts — pure sim nondeterminism. No visible change in the candidate area (minimap identical, no order markers in either set, same panning/framing) and no regressions in either set, so the verdict is a tie. Neither set shows order-marker visuals at all, and both share the same weak minimap presentation (unframed, low-res blob terrain over a visible grid, mushy fog edge, viewport indicator missing when zoomed out)."
- **S1 ronda 1** (alisado, run_ok=true): harness en mapas 1/2/4 (Standard, Volcanic Coast, Islands) sin líneas SCRIPT ERROR; los 12 PNG muestran una escena isométrica coherente (diamante 2:1, entidades verticales, decales de suelo proyectados). Incoherencias encontradas y corregidas: (1) las sombras de suelo se dibujaban con la elipse y el offset autorales en espacio de mundo, con lo que tras la proyección quedaban inclinadas ~27° y desplazadas en diagonal respecto a los pies — ahora `VisualFx.add_ground_shadow` genera la preimagen mundo de la elipse de pantalla deseada y la sombra asienta horizontal bajo los pies (todas las unidades, edificios y animales); (2) la oveja convertida se repintaba el torso entero del color del equipo y se leía como un bloque azul — ahora usa la misma franja de color en los pies (vertical/upright) que el resto de unidades, recoloreable al cambiar de dueño; (3) el rectángulo de cámara del minimapa se dibujaba alineado a ejes ignorando la rotación de la cámara — ahora se proyectan las 4 esquinas de pantalla a mundo y se dibuja el rombo real (con clip en el widget); (4) el asta del banderín de rally se proyectaba plana como un trazo diagonal — ahora se levanta con `IsoBillboard.make_upright` dejando el aro base como decal de suelo; (5) barra "0%" huérfana visible en el panel inferior sin selección — se oculta cuando no hay valor. Diferido: banda recta de bajíos en el borde del mapa de Volcanic Coast (rasgo del generador, no de la proyección) y el leve solape del rótulo del Town Center con las almenas. Tests 258/258 OK; harness re-ejecutado en mapas 1 y 2 confirmando visualmente cada arreglo.
- **P0 ronda 3** (nueva GANA, run_ok=true): "Harness exited 0 with no SCRIPT ERROR lines (only benign Camera2D/navmesh warnings). Blind comparison: Set B is the isometric candidate — 03_overview shows a proper 2:1 diamond map silhouette, and 01/02/04 show a foreshortened ground plane that fills the viewport with coherent composition, readable upright units/trees, and a working minimap/HUD. Set A reads as plain top-down: circular map floating in black, huge dead-space frames in 02/03 (03 is ~70% black screen), tiny sprites. B wins every shot on composition, screen usage, and genre feel. B's remaining issues beyond the building billboard gap: (1) unit/animal selection plinths are flat blue horizontal rectangles instead of ground-aligned ellipses/diamonds, so units look like they stand on floating bars; (2) far-left edge of 02_tc_medium shows smeared grey/white parallelogram shapes (sheared rock sprites) hinting the projection is a shear applied to sprites near edges; (3) top edge of 01_tc_close clips some yellow shapes and a sheep against the HUD bar. No hard regressions in B: HUD intact, no black frames, units visible, minimap renders. Set A shot 03 has severe black-frame composition but that is the non-candidate baseline." — mayor hueco: "Buildings are still flat, front-facing billboards: the Town Center is a square facade sitting on a horizontal rectangular base that ignores the projected ground. It needs an isometric massing — a diamond footprint with two visible side faces (roof plane + shaded walls) so it occupies the same 2:1 diamond space as the terrain. This is the single element that most breaks the isometric illusion, since the building is the focal point of every shot."
- **P0 ronda 2** (nueva PIERDE, run_ok=true): "Harness exit 0, no SCRIPT ERROR (only benign Camera2D/navmesh warnings). Set A = /tmp/calima-iso/p0-r2 (isometric candidate, diamond ground plane); Set B = baseline top-down. A's diamond ground with stair-step tile edges is a step in the right direction, but the whole scene is sheared: units lean diagonally, selection bases become slashes, a unit stands on the TC roof, and the TC sprite is washed-out/semi-transparent with ghost HP pills. Composition regressions in A: 02_tc_medium shows a small map floating in black; 03_overview is ~90% black with a thumbnail-sized map plus a stray dark-grey background diamond — illegible. B's 03_overview is also badly framed (map crammed bottom-right, mostly black), so the shared overview zoom needs fixing in both. HUD present in all shots; no fully black frames; no invisible units in B. Verdict: clean top-down B beats A's currently broken projection." — mayor hueco: "Units/buildings inherit the ground shear: sprites (villagers, cavalry, sheep, TC, selection bases) are skewed ~40° and read as falling over. Render them as upright, feet-anchored billboards on top of the projected diamond ground (counter-shear or draw in an unprojected layer) so only terrain gets the isometric transform."
- **P0 ronda 1** (nueva PIERDE, run_ok=true): "Harness ran clean: exit 0, no SCRIPT ERROR lines, all 4 PNGs saved to /tmp/calima-iso/p0-r1. Blind verdict: Set B is the isometric candidate (diamond ground plane). Its diamond orientation is correct and terrain blending survives, but the whole scene is flattened into the ground plane with no verticality, making the TC and units nearly illegible. Regressions in B beyond the flattening: (1) framing broken — 01_tc_close is not a close shot, it shows the whole map at far distance, so there is no close-up view at all; (2) 03_overview shrinks the playfield to a small patch with a large dark-gray diamond backdrop polygon artifact filling the frame; (3) units are so small/flattened they are effectively invisible in every B shot, and the TC label text is skewed/illegible. Set A is flat top-down but has readable upright buildings, visible units with health bars, correct per-shot framing, and intact HUD. Per the bar (broken/illegible projection loses to clean top-down), A wins. HUD, minimap, and resource bar are present and intact in both sets; no black frames or missing HUD." — mayor hueco: "Everything on the map — buildings, units, trees — is skewed flat with the terrain transform instead of standing upright on it. The TC reads as a gray floor slab and trees as pancakes. Render entities as upright, counter-transformed (billboard-style) sprites anchored to and Y-sorted on the projected diamond ground so they have vertical height, while only the ground plane carries the isometric skew."

_(los agentes añaden entradas aquí, la más reciente arriba)_
